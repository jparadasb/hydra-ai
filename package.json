{
  "name": "hydra-ai-site",
  "private": true,
  "description": "Build tooling for the landing page served by the coordinator (Tailwind v4)",
  "scripts": {
    "build:site": "tailwindcss -i coordinator/assets/styles.css -o coordinator/priv/site/tailwind.css --minify",
    "watch:site": "tailwindcss -i coordinator/assets/styles.css -o coordinator/priv/site/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
