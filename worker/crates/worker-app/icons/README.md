# icons

Generated by `cargo tauri icon path/to/logo.png` (not committed — produced from your source
logo). The bundle config in `../tauri.conf.json` expects `32x32.png`, `128x128.png`,
`icon.icns`, and `icon.ico` here. `cargo tauri dev` runs without them.
