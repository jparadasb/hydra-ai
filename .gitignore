# Rust
/worker/target/
**/*.rs.bk

# Elixir
/coordinator/_build/
/coordinator/deps/
/coordinator/*.ez
erl_crash.dump

# SQLite databases
/coordinator/*.db
/coordinator/*.db-shm
/coordinator/*.db-wal

# Local secrets
.env

# Local worker state (never commit secrets)
*.vault.bin
.hydra-vault.bin
.hydra-usage.json
